{
  "name": "kcls-museum-passes",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "start": "cd apps/api && node dist/index.js",
    "dev": "npm run build:api && concurrently \"node apps/api/dist/index.js\" \"npm run dev --prefix apps/web\"",
    "dev:watch": "concurrently \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "dev:compiled": "npm run build:api && concurrently \"node apps/api/dist/index.js\" \"npm run dev --prefix apps/web\"",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "build:api": "npm run build --prefix apps/api",
    "build:web": "npm run build --prefix apps/web",
    "copy:web": "rm -rf apps/api/public && mkdir -p apps/api/public && cp -r apps/web/dist/* apps/api/public",
    "build:prod": "npm run build:web && npm run copy:web && npm run build:api",
    "start:prod": "NODE_ENV=production node apps/api/dist/index.js",
    "serve": "npm run build:prod && npm run start:prod",
    "deploy:local": "npm run build:prod && sudo systemctl restart kcls-museum.service"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
